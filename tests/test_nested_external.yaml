openapi: 3.0.0
info:
  title: Main spec for nested external refs test
  version: 1.0.0
servers:
  - url: https://api.example.com
paths:
  /test:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: 'subdir/schema_a.yaml#/components/schemas/SchemaA'
      responses:
        '200':
          description: OK
